class Eventchain {
	constructor(aliases, events, nextRoom) {
		var self = this;
		this.aliases = aliases;
	   	this.events = events;
		this.nextRoom = nextRoom;
	}
			
    animation(data) {
       
    }
}